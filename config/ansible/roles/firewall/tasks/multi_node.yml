---
- name: "Deploy multi-node firewall rules"
  become: true
  ansible.builtin.template:
    src: firewall-multi.nft.j2
    dest: /etc/lantana/honeywall/nftables/firewall.nft
    owner: root
    group: root
    mode: "0600"
    validate: /usr/sbin/nft -c -f %s
  notify: "Reload nftables"
  tags: [nftables]
